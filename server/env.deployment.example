# Deployment Environment Variables (Render/Production)
# Copy this file to .env for deployment
# This configuration is for running PostgreSQL on Render/Supabase

PORT=4000

# Render PostgreSQL Connection
# Option 1: Use DATABASE_URL (recommended for Render)
DATABASE_URL=postgresql://user:password@host:port/database

# Option 2: Use individual connection parameters
# DB_HOST=your-render-db-host
# DB_PORT=5432
# DB_USER=your-db-user
# DB_PASSWORD=your-db-password
# DB_NAME=your-db-name

# SSL is automatically enabled for remote hosts
# You can explicitly set it if needed:
# DB_SSL=true

# Exam Configuration
EXAM_PASSWORD=DC-ML@25
SESSION_DURATION_MINUTES=60

# CORS - Allow Netlify frontend
ALLOWED_ORIGINS=https://your-netlify-site.netlify.app

# Database Connection Pool (optional, defaults shown)
# DB_POOL_MAX=20
# DB_POOL_MIN=5

